# Godot 4 — nur den Editor-/Import-Cache ignorieren.
# WICHTIG: Die `*.import`-Dateien NEBEN den Assets werden versioniert (enthalten uid &
# Import-Einstellungen) — daher hier bewusst NICHT ignorieren.
.godot/

# Export-Artefakte
export.cfg
export_presets.cfg
*.translation
